; Include Drupal Core and any core patches.

api = 2
core = 8.x

; Include the definition for how to build Drupal core directly:
includes[] = drupal-org-core.make

# ; {{DISTRO}} install profile
# projects[{{DISTRO}}][type] = profile
# projects[{{DISTRO}}][download][type] = git
# projects[{{DISTRO}}][download][url] = "git@github.com:USERNAME/{{DISTRO}}.git"
#


